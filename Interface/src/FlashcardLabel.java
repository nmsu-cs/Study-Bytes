import javax.swing.*;

public class FlashcardLabel extends JLabel
{
    /**
     * This class acts as the format for one side of a flashcard, to be used as a label in FlashcardFrame
     */

    public FlashcardLabel(String text)
    {
        this.setText(text);
    }
}